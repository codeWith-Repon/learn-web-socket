## 🧐 WebSocket কী?

WebSocket হলো একটি **স্টেটফুল কমিউনিকেশন প্রোটোকল** যা ক্লায়েন্ট এবং সার্ভারের মধ্যে একটি স্থায়ী সংযোগের মাধ্যমে রিয়েল-টাইম, দ্বিমুখী ডেটা আদান-প্রদান সক্ষম করে। প্রচলিত HTTP-এর বিপরীতে, WebSocket **ফুল-ডুপ্লেক্স কমিউনিকেশন** সমর্থন করে, যা উভয় পক্ষকে একসাথে ডেটা পাঠাতে এবং গ্রহণ করতে দেয় - বারবার কানেকশন স্থাপনের ওভারহেড ছাড়াই।

### ⚡ HTTP বনাম WebSocket: তুলনামূলক বিশ্লেষণ

| বৈশিষ্ট্য             | HTTP (Stateless)                                              | WebSocket (Stateful)                                                                       |
| --------------------- | ------------------------------------------------------------- | ------------------------------------------------------------------------------------------ |
| **কানেকশন মডেল**      | প্রতিটি রিকোয়েস্টে নতুন কানেকশন তৈরি হয়                     | প্রাথমিক হ্যান্ডশেকের পর স্থায়ী সংযোগ বজায় থাকে                                          |
| **যোগাযোগ প্যাটার্ন** | একমুখী (শুধুমাত্র ক্লায়েন্ট-প্রারম্ভিক রিকোয়েস্ট)           | দ্বিমুখী (ক্লায়েন্ট এবং সার্ভার উভয়ই ডেটা ট্রান্সফার শুরু করতে পারে)                     |
| **লেটেন্সি**          | কানেকশন ওভারহেডের কারণে উচ্চতর লেটেন্সি                       | রিয়েল-টাইম অ্যাপ্লিকেশনের জন্য অত্যন্ত কম লেটেন্সি                                        |
| **ব্যবহারের ক্ষেত্র** | স্ট্যাটিক কন্টেন্ট ডেলিভারি, RESTful API, ঐতিহ্যবাহী ওয়েবপেজ | চ্যাট অ্যাপ্লিকেশন, লাইভ নোটিফিকেশন, মাল্টিপ্লেয়ার গেম, লাইভ ড্যাশবোর্ড, কোলাবরেটিভ এডিটর |
| **ডেটা ফরম্যাট**      | টেক্সট (JSON, HTML, XML)                                      | টেক্সট এবং বাইনারি উভয়ই সমর্থিত                                                           |
| **সার্ভার পুশ**       | সমর্থিত নয় (পোলিং প্রয়োজন)                                  | নেটিভ সমর্থন (সার্ভার যেকোনো সময় ডেটা পুশ করতে পারে)                                      |

### 🔄 WebSocket লাইফ সাইকেল

WebSocket সংযোগ চারটি প্রধান পর্যায়ের মধ্য দিয়ে যায়:

**১. হ্যান্ডশেক (Handshake)**

- HTTP থেকে WebSocket প্রোটোকলে আপগ্রেড করার জন্য প্রাথমিক HTTP রিকোয়েস্ট
- সার্ভার `101 Switching Protocols` স্ট্যাটাস কোড দিয়ে সাড়া দেয়
- `Upgrade: websocket` এবং `Connection: Upgrade` হেডার ব্যবহৃত হয়

**২. ওপেন (Open)**

- কানেকশন সক্রিয় এবং ডেটা ট্রান্সমিশনের জন্য প্রস্তুত
- `onopen` ইভেন্ট ট্রিগার হয়
- এই মুহূর্ত থেকে উভয় পক্ষ মেসেজ পাঠাতে পারে

**৩. ডেটা ট্রান্সফার (Data Transfer)**

- টেক্সট বা বাইনারি ডেটা ফ্রেম আকারে আদান-প্রদান হয়
- `onmessage` ইভেন্ট প্রতিটি ইনকামিং মেসেজে ট্রিগার হয়
- কোন অতিরিক্ত HTTP ওভারহেড ছাড়াই ডেটা প্রবাহিত হয়

**৪. ক্লোজ (Close)**

- যেকোনো পক্ষ সুশৃঙ্খলভাবে সংযোগ বন্ধ করতে পারে
- ক্লোজিং হ্যান্ডশেক সম্পন্ন হয়
- `onclose` ইভেন্ট ট্রিগার হয় এবং সংযোগ শেষ হয়

---

## 🛠️ সার্ভার সেটআপ (Node.js `ws` লাইব্রেরি)

### ইনস্টলেশন

প্রথমে `ws` লাইব্রেরি ইনস্টল করুন:

```bash
npm install ws
```

### সার্ভার ইমপ্লিমেন্টেশন

**`server.js`**

```javascript
import { WebSocketServer, WebSocket } from 'ws';

// WebSocket সার্ভার তৈরি করা
const wss = new WebSocketServer({ port: 8080 });

// নতুন ক্লায়েন্ট কানেকশন হ্যান্ডল করা
wss.on('connection', (socket, request) => {
  const ip = request.socket.remoteAddress;
  console.log(`🚀 নতুন সংযোগ স্থাপিত: ${ip}`);

  // ক্লায়েন্ট থেকে মেসেজ গ্রহণ করা
  socket.on('message', (rawData) => {
    const message = rawData.toString();
    console.log(`📩 প্রাপ্ত মেসেজ: ${message}`);

    // সকল সংযুক্ত ক্লায়েন্টদের কাছে মেসেজ ব্রডকাস্ট করা
    wss.clients.forEach((client) => {
      if (client.readyState === WebSocket.OPEN) {
        client.send(`সার্ভার ব্রডকাস্ট: ${message}`);
      }
    });
  });

  // ক্লায়েন্ট ডিসকানেক্ট হ্যান্ডল করা
  socket.on('close', () => {
    console.log('👋 ক্লায়েন্ট সংযোগ বিচ্ছিন্ন হয়েছে');
  });

  // এরর হ্যান্ডলিং
  socket.on('error', (error) => {
    console.error('❌ সংযোগ ত্রুটি:', error);
  });
});

console.log('✅ WebSocket সার্ভার চালু আছে: ws://localhost:8080');
```

---

## 💻 ক্লায়েন্ট ইমপ্লিমেন্টেশন

### ব্রাউজার-ভিত্তিক ক্লায়েন্ট

ব্রাউজার কনসোলে অথবা আপনার জাভাস্ক্রিপ্ট ফাইলে নিচের কোড ব্যবহার করুন:

```javascript
// WebSocket সংযোগ তৈরি করা
const socket = new WebSocket('ws://localhost:8080');

// সংযোগ সফল হলে
socket.onopen = () => {
  console.log('✅ সার্ভারের সাথে সংযুক্ত হয়েছে!');
  socket.send('হ্যালো সার্ভার, আমি ব্রাউজার থেকে মেসেজ পাঠাচ্ছি!');
};

// সার্ভার থেকে মেসেজ গ্রহণ করা
socket.onmessage = (event) => {
  console.log('📨 সার্ভার থেকে মেসেজ:', event.data);
};

// সংযোগ বিচ্ছিন্ন হলে
socket.onclose = (event) => {
  console.log('🔌 সংযোগ বন্ধ হয়েছে। কোড:', event.code);
};

// এরর হ্যান্ডলিং
socket.onerror = (error) => {
  console.error('❌ সংযোগ ত্রুটি:', error);
};
```

---

## 📚 গুরুত্বপূর্ণ পরিভাষা (Key Terminology)

### **ঘোস্ট কানেকশন (Ghost Connection)**

এমন পরিস্থিতি যেখানে ক্লায়েন্ট সংযোগ বিচ্ছিন্ন হয়ে যায় কিন্তু সার্ভার তা সনাক্ত করতে পারে না, যার ফলে অব্যবহৃত সংযোগ খোলা থেকে যায়।

**সমাধান:** হার্টবিট মেকানিজম (Ping/Pong) ব্যবহার করে নিয়মিত সংযোগের স্বাস্থ্য পরীক্ষা করা।

```javascript
// হার্টবিট উদাহরণ
setInterval(() => {
  wss.clients.forEach((client) => {
    if (client.isAlive === false) return client.terminate();
    client.isAlive = false;
    client.ping();
  });
}, 30000);
```

### **ব্যাকপ্রেশার (Backpressure)**

এমন অবস্থা যখন ডেটা পাঠানোর হার ডেটা প্রসেস করার হারের চেয়ে বেশি হয়ে যায়, যার ফলে বাফার ওভারফ্লো হতে পারে।

**সমাধান:** বাফার লিমিট সেট করা এবং ব্যাকপ্রেশার পরিচালনা করা।

### **অপকোড (Opcode)**

WebSocket ফ্রেমের টাইপ নির্দেশক যা ডেটার প্রকৃতি চিহ্নিত করে:

- `0x0` - Continuation Frame
- `0x1` - Text Frame
- `0x2` - Binary Frame
- `0x8` - Connection Close
- `0x9` - Ping
- `0xA` - Pong

### **CSP এরর (Content Security Policy Error)**

ব্রাউজারের নিরাপত্তা নীতি যা ক্রস-অরিজিন WebSocket সংযোগ ব্লক করতে পারে।

**সমাধান:** সার্ভারে যথাযথ CORS হেডার এবং CSP ডিরেক্টিভ কনফিগার করা:

```javascript
'Content-Security-Policy': "connect-src 'self' ws://localhost:8080"
```

---

## 🎯 বেস্ট প্র্যাকটিস

- ✅ **সবসময় এরর হ্যান্ডলিং ইমপ্লিমেন্ট করুন**
- ✅ **হার্টবিট মেকানিজম ব্যবহার করে ঘোস্ট কানেকশন প্রতিরোধ করুন**
- ✅ **সংবেদনশীল ডেটার জন্য `wss://` (WebSocket Secure) ব্যবহার করুন**
- ✅ **রিকানেকশন লজিক যোগ করুন সংযোগ বিচ্ছিন্ন হওয়ার ক্ষেত্রে**
- ✅ **মেসেজ সাইজ লিমিট সেট করে DoS আক্রমণ প্রতিরোধ করুন**

## 🎯 এনভেলপ প্যাটার্ন (Envelope Pattern)

এনভেলপ প্যাটার্ন হলো WebSocket অ্যাপ্লিকেশনে ডেটা ট্রান্সমিশনের একটি **স্ট্যান্ডার্ড এবং প্রফেশনাল আর্কিটেকচারাল প্যাটার্ন**। রিয়েল-টাইম অ্যাপ্লিকেশন (যেমন চ্যাট, লাইভ নোটিফিকেশন, কোলাবরেটিভ এডিটর) ডেভেলপ করার সময়, শুধুমাত্র raw টেক্সট পাঠানো যথেষ্ট নয়। আমাদের প্রয়োজন মেসেজের **টাইপ, মেটাডেটা এবং প্রসঙ্গ** (context) সহ সুসংগঠিত ডেটা স্ট্রাকচার।

এনভেলপ প্যাটার্ন এই সমস্যার সমাধান করে প্রতিটি মেসেজকে একটি **স্ট্যান্ডার্ডাইজড র‍্যাপার** বা "খামে" মোড়ানোর মাধ্যমে।

---

### 🔍 সমস্যা চিহ্নিতকরণ (The Problem)

প্রচলিত WebSocket ইমপ্লিমেন্টেশনে যখন আপনি এভাবে ডেটা পাঠান:

```javascript
socket.send('Hello');
```

সার্ভার শুধুমাত্র `"Hello"` স্ট্রিং পায়। কিন্তু এখানে বেশ কিছু সমস্যা রয়েছে:

❌ **টাইপ অস্পষ্টতা:** এটি কি চ্যাট মেসেজ, টাইপিং ইন্ডিকেটর, নাকি নোটিফিকেশন?  
❌ **প্রসঙ্গ অনুপস্থিতি:** মেসেজটি কে পাঠিয়েছে? কোন রুমে? কখন?  
❌ **স্কেলেবিলিটি ইস্যু:** নতুন ফিচার যোগ করা কঠিন হয়ে পড়ে  
❌ **এরর হ্যান্ডলিং জটিল:** সমস্যা ডিবাগ করা এবং ভ্যালিডেশন করা কষ্টসাধ্য

---

### 📦 এনভেলপ স্ট্রাকচার (Envelope Structure)

এনভেলপ প্যাটার্নে প্রতিটি মেসেজ একটি **সুসংগঠিত JSON অবজেক্ট** হিসেবে পাঠানো হয়। একটি স্ট্যান্ডার্ড এনভেলপে সাধারণত নিম্নলিখিত ফিল্ডগুলো থাকে:

#### মৌলিক স্ট্রাকচার

```javascript
{
  "type": "EVENT_TYPE",           // মেসেজের ধরণ (বাধ্যতামূলক)
  "payload": { ... },              // আসল ডেটা (বাধ্যতামূলক)
  "metadata": { ... }              // অতিরিক্ত তথ্য (ঐচ্ছিক)
}
```

#### ফিল্ডসমূহের বিস্তারিত ব্যাখ্যা

| ফিল্ড          | বর্ণনা                                                 | উদাহরণ                                                  |
| -------------- | ------------------------------------------------------ | ------------------------------------------------------- |
| **`type`**     | মেসেজের শ্রেণী নির্ধারণ করে (সাধারণত UPPER_SNAKE_CASE) | `CHAT_MESSAGE`, `USER_JOINED`, `TYPING_START`           |
| **`payload`**  | প্রকৃত ডেটা যা ট্রান্সমিট করতে হবে                     | `{ text: "Hello", userId: "123" }`                      |
| **`metadata`** | টাইমস্ট্যাম্প, requestId, version ইত্যাদি              | `{ timestamp: "2026-01-31T12:00:00Z", version: "1.0" }` |

---

### 💡 প্র্যাকটিক্যাল উদাহরণ

#### উদাহরণ ১: চ্যাট মেসেজ

```javascript
{
  "type": "CHAT_MESSAGE",
  "payload": {
    "sender": "Shakil",
    "senderId": "user_123",
    "text": "How are you?",
    "roomId": "group_123"
  },
  "metadata": {
    "timestamp": "2026-01-31T12:00:00Z",
    "messageId": "msg_456",
    "version": "1.0"
  }
}
```

#### উদাহরণ ২: টাইপিং ইন্ডিকেটর

```javascript
{
  "type": "TYPING_START",
  "payload": {
    "userId": "user_789",
    "userName": "Rahim",
    "roomId": "group_123"
  },
  "metadata": {
    "timestamp": "2026-01-31T12:01:30Z"
  }
}
```

#### উদাহরণ ৩: ফাইল আপলোড নোটিফিকেশন

```javascript
{
  "type": "FILE_UPLOADED",
  "payload": {
    "fileName": "document.pdf",
    "fileSize": 2048576,
    "uploadedBy": "user_456",
    "fileUrl": "https://cdn.example.com/files/doc.pdf"
  },
  "metadata": {
    "timestamp": "2026-01-31T12:05:00Z",
    "uploadId": "upload_789"
  }
}
```

---

### 🖥️ সার্ভার সাইড ইমপ্লিমেন্টেশন

#### বেসিক হ্যান্ডলার

```javascript
import { WebSocketServer, WebSocket } from 'ws';

const wss = new WebSocketServer({ port: 8080 });

wss.on('connection', (socket, request) => {
  console.log('🔌 নতুন ক্লায়েন্ট সংযুক্ত হয়েছে');

  socket.on('message', (rawData) => {
    try {
      // এনভেলপ পার্স করা
      const envelope = JSON.parse(rawData.toString());

      // ভ্যালিডেশন
      if (!envelope.type || !envelope.payload) {
        socket.send(
          JSON.stringify({
            type: 'ERROR',
            payload: { message: 'Invalid envelope structure' },
          }),
        );
        return;
      }

      // টাইপ অনুযায়ী রাউটিং
      handleMessage(envelope, socket, wss);
    } catch (error) {
      console.error('❌ JSON পার্স এরর:', error);
      socket.send(
        JSON.stringify({
          type: 'ERROR',
          payload: { message: 'Invalid JSON format' },
        }),
      );
    }
  });
});

// মেসেজ হ্যান্ডলার ফাংশন
function handleMessage(envelope, socket, wss) {
  switch (envelope.type) {
    case 'CHAT_MESSAGE':
      handleChatMessage(envelope.payload, wss);
      break;

    case 'TYPING_START':
      handleTypingIndicator(envelope.payload, wss, true);
      break;

    case 'TYPING_STOP':
      handleTypingIndicator(envelope.payload, wss, false);
      break;

    case 'USER_JOINED':
      handleUserJoined(envelope.payload, wss);
      break;

    case 'USER_LEFT':
      handleUserLeft(envelope.payload, wss);
      break;

    default:
      console.warn('⚠️ অজানা মেসেজ টাইপ:', envelope.type);
      socket.send(
        JSON.stringify({
          type: 'ERROR',
          payload: { message: `Unknown message type: ${envelope.type}` },
        }),
      );
  }
}

// চ্যাট মেসেজ ব্রডকাস্ট করা
function handleChatMessage(payload, wss) {
  const response = {
    type: 'CHAT_MESSAGE',
    payload: {
      ...payload,
      serverProcessedAt: new Date().toISOString(),
    },
  };

  // নির্দিষ্ট রুমে ব্রডকাস্ট
  wss.clients.forEach((client) => {
    if (
      client.readyState === WebSocket.OPEN &&
      client.roomId === payload.roomId
    ) {
      client.send(JSON.stringify(response));
    }
  });
}

// টাইপিং ইন্ডিকেটর হ্যান্ডলার
function handleTypingIndicator(payload, wss, isTyping) {
  const response = {
    type: isTyping ? 'TYPING_START' : 'TYPING_STOP',
    payload: payload,
  };

  wss.clients.forEach((client) => {
    if (
      client.readyState === WebSocket.OPEN &&
      client.roomId === payload.roomId &&
      client.userId !== payload.userId
    ) {
      client.send(JSON.stringify(response));
    }
  });
}
```

---

### 💻 ক্লায়েন্ট সাইড ইমপ্লিমেন্টেশন

#### হেল্পার ফাংশন তৈরি

```javascript
class WebSocketClient {
  constructor(url) {
    this.socket = new WebSocket(url);
    this.handlers = new Map();

    this.socket.onmessage = (event) => {
      try {
        const envelope = JSON.parse(event.data);
        const handler = this.handlers.get(envelope.type);

        if (handler) {
          handler(envelope.payload, envelope.metadata);
        } else {
          console.warn('⚠️ কোনো হ্যান্ডলার পাওয়া যায়নি:', envelope.type);
        }
      } catch (error) {
        console.error('❌ মেসেজ প্রসেস এরর:', error);
      }
    };
  }

  // মেসেজ পাঠানোর সহজ মেথড
  send(type, payload, metadata = {}) {
    const envelope = {
      type,
      payload,
      metadata: {
        ...metadata,
        timestamp: new Date().toISOString(),
      },
    };

    this.socket.send(JSON.stringify(envelope));
  }

  // ইভেন্ট হ্যান্ডলার রেজিস্টার করা
  on(type, handler) {
    this.handlers.set(type, handler);
  }
}

// ব্যবহার
const client = new WebSocketClient('ws://localhost:8080');

// হ্যান্ডলার রেজিস্টার করা
client.on('CHAT_MESSAGE', (payload, metadata) => {
  console.log(`${payload.sender}: ${payload.text}`);
  displayMessage(payload);
});

client.on('TYPING_START', (payload) => {
  showTypingIndicator(payload.userName);
});

// মেসেজ পাঠানো
client.send('CHAT_MESSAGE', {
  sender: 'Shakil',
  senderId: 'user_123',
  text: 'Hello everyone!',
  roomId: 'group_123',
});

// টাইপিং ইভেন্ট পাঠানো
client.send('TYPING_START', {
  userId: 'user_123',
  userName: 'Shakil',
  roomId: 'group_123',
});
```

---

### ✅ এনভেলপ প্যাটার্নের সুবিধা

| সুবিধা                              | বর্ণনা                                                   |
| ----------------------------------- | -------------------------------------------------------- |
| **🚀 স্কেলেবিলিটি**                 | নতুন ফিচার যোগ করা সহজ - শুধু নতুন `type` যুক্ত করুন     |
| **🎯 টাইপ সেফটি**                   | TypeScript/Flow দিয়ে স্ট্রং টাইপিং সম্ভব                |
| **🔍 ডিবাগিং**                      | লগ দেখে সহজেই বুঝা যায় কী ধরনের মেসেজ আদান-প্রদান হচ্ছে |
| **📊 মনিটরিং**                      | মেসেজ টাইপ অনুযায়ী মেট্রিক্স সংগ্রহ করা যায়            |
| **🛡️ ভ্যালিডেশন**                   | প্রতিটি টাইপের জন্য আলাদা স্কিমা ভ্যালিডেশন সম্ভব        |
| **🔄 ব্যাকওয়ার্ড কম্প্যাটিবিলিটি** | `version` ফিল্ড দিয়ে পুরোনো ক্লায়েন্ট সাপোর্ট করা যায় |
| **📝 ডকুমেন্টেশন**                  | API ডকুমেন্ট করা এবং বুঝা সহজ হয়                        |

---

### 🎨 অ্যাডভান্সড প্যাটার্ন: Request-Response

কিছু ক্ষেত্রে আপনি চাইতে পারেন একটি রিকোয়েস্টের জন্য সুনির্দিষ্ট রেসপন্স পেতে:

```javascript
// ক্লায়েন্ট সাইড
const requestId = generateUniqueId();

client.send(
  'GET_USER_PROFILE',
  {
    userId: 'user_456',
  },
  {
    requestId: requestId,
  },
);

client.on('USER_PROFILE_RESPONSE', (payload, metadata) => {
  if (metadata.requestId === requestId) {
    console.log('প্রোফাইল ডেটা:', payload);
  }
});
```

---

### 📋 বেস্ট প্র্যাকটিস

#### ১. টাইপ নেমিং কনভেনশন

```javascript
// ✅ ভালো - বর্ণনামূলক এবং সুসংগত
'USER_JOINED';
'MESSAGE_SENT';
'NOTIFICATION_RECEIVED';

// ❌ খারাপ - অস্পষ্ট বা অসামঞ্জস্যপূর্ণ
'joined';
'msg';
'notif_rcv';
```

#### ২. পেলোড ভ্যালিডেশন

```javascript
// স্কিমা ভ্যালিডেশন লাইব্রেরি ব্যবহার করুন (যেমন: Joi, Zod)
import Joi from 'joi';

const chatMessageSchema = Joi.object({
  sender: Joi.string().required(),
  senderId: Joi.string().required(),
  text: Joi.string().max(1000).required(),
  roomId: Joi.string().required(),
});

function validatePayload(payload, schema) {
  const { error } = schema.validate(payload);
  if (error) {
    throw new Error(`ভ্যালিডেশন ব্যর্থ: ${error.message}`);
  }
}
```

#### ৩. এরর হ্যান্ডলিং স্ট্যান্ডার্ড

```javascript
// সকল এরর এই ফরম্যাটে পাঠান
{
  "type": "ERROR",
  "payload": {
    "code": "INVALID_MESSAGE",
    "message": "মেসেজ ফরম্যাট ভুল",
    "details": { ... }
  },
  "metadata": {
    "timestamp": "2026-01-31T12:00:00Z",
    "originalType": "CHAT_MESSAGE"
  }
}
```

#### ৪. টাইমস্ট্যাম্প সবসময় ISO 8601 ফরম্যাটে

```javascript
const timestamp = new Date().toISOString(); // "2026-01-31T12:00:00.000Z"
```

---

### 🔧 টাইপস্ক্রিপ্ট ইন্টিগ্রেশন

```typescript
// টাইপ ডেফিনিশন
interface Envelope<T = any> {
  type: string;
  payload: T;
  metadata?: {
    timestamp?: string;
    requestId?: string;
    version?: string;
    [key: string]: any;
  };
}

interface ChatMessagePayload {
  sender: string;
  senderId: string;
  text: string;
  roomId: string;
}

// টাইপ-সেফ মেসেজ পাঠানো
function sendChatMessage(client: WebSocket, payload: ChatMessagePayload) {
  const envelope: Envelope<ChatMessagePayload> = {
    type: 'CHAT_MESSAGE',
    payload,
    metadata: {
      timestamp: new Date().toISOString(),
    },
  };

  client.send(JSON.stringify(envelope));
}
```

---

### 📊 মনিটরিং এবং লগিং

```javascript
// মেসেজ টাইপ অনুযায়ী কাউন্টার
const messageStats = new Map();

function logMessage(envelope) {
  const count = messageStats.get(envelope.type) || 0;
  messageStats.set(envelope.type, count + 1);

  console.log(`📈 মেসেজ স্ট্যাট:`, Object.fromEntries(messageStats));
}

// পারফরম্যান্স ট্র্যাকিং
function trackMessageLatency(envelope) {
  if (envelope.metadata?.timestamp) {
    const sent = new Date(envelope.metadata.timestamp);
    const received = new Date();
    const latency = received - sent;

    console.log(`⏱️ লেটেন্সি (${envelope.type}): ${latency}ms`);
  }
}
```

## ✅ অ্যাকনলেজমেন্ট (Acknowledgement - ACK)

WebSocket কমিউনিকেশনে **অ্যাকনলেজমেন্ট (ACK)** হলো একটি **নিশ্চিতকরণ মেকানিজম** যা প্রেরক (sender) কে জানায় যে তার পাঠানো মেসেজটি সফলভাবে গ্রহীতার (receiver) কাছে পৌঁছেছে এবং প্রসেস হয়েছে। এটি অনেকটা **WhatsApp-এর Double Tick (✓✓)** বা **iMessage-এর Delivered স্ট্যাটাস** এর মতো কাজ করে।

বাস্তব জীবনে, ACK মেকানিজম নিশ্চিত করে যে গুরুত্বপূর্ণ ডেটা (যেমন: পেমেন্ট ট্রানজেকশন, অর্ডার কনফার্মেশন, চ্যাট মেসেজ) সফলভাবে ডেলিভার হয়েছে।

---

### 🤔 কেন অ্যাকনলেজমেন্ট প্রয়োজন?

WebSocket মূলত একটি **"Fire-and-Forget"** প্রোটোকল। এর অর্থ হলো:

```javascript
socket.send('Hello World'); // মেসেজ পাঠানো হলো
// কিন্তু এটি পৌঁছেছে কি না? ❓
```

#### সমস্যাসমূহ:

❌ **নেটওয়ার্ক ফেইলিউর:** দুর্বল ইন্টারনেট কানেকশনে মেসেজ হারিয়ে যেতে পারে  
❌ **সার্ভার ওভারলোড:** সার্ভার ব্যস্ত থাকলে মেসেজ ড্রপ হতে পারে  
❌ **ক্লায়েন্ট অনিশ্চয়তা:** প্রেরক জানে না মেসেজ ডেলিভার হয়েছে কি না  
❌ **ডুপ্লিকেট মেসেজ:** একই মেসেজ বারবার পাঠানো হতে পারে  
❌ **ডেটা ইন্টিগ্রিটি:** গুরুত্বপূর্ণ ডেটা হারানোর ঝুঁকি

**Native WebSocket-এ এই সমস্যার সমাধানের কোনো বিল্ট-ইন মেকানিজম নেই।** তাই আমাদের নিজেদের ACK সিস্টেম ইমপ্লিমেন্ট করতে হয়।

---

### ⚙️ ACK মেকানিজম: কীভাবে কাজ করে?

ACK সিস্টেম একটি **ID-ভিত্তিক Request-Response প্যাটার্ন** অনুসরণ করে:

```
┌─────────┐                                    ┌─────────┐
│ Client  │                                    │ Server  │
└────┬────┘                                    └────┬────┘
     │                                              │
     │  1️⃣ MESSAGE (id: msg_101)                   │
     │─────────────────────────────────────────────>│
     │                                              │
     │                                         2️⃣ প্রসেস
     │                                              │
     │  3️⃣ ACK (id: msg_101, status: DELIVERED)    │
     │<─────────────────────────────────────────────│
     │                                              │
  4️⃣ নিশ্চিত ✓                                    │
     │                                              │
```

#### ধাপসমূহ:

**১. ক্লায়েন্ট:** ইউনিক `messageId` সহ মেসেজ পাঠায়  
**২. সার্ভার:** মেসেজ রিসিভ করে এবং প্রসেস করে  
**৩. সার্ভার:** একই `messageId` দিয়ে ACK পাঠায়  
**৪. ক্লায়েন্ট:** ACK পেয়ে নিশ্চিত হয় যে মেসেজ ডেলিভার হয়েছে

---

### 📦 এনভেলপ প্যাটার্নে ACK ইমপ্লিমেন্টেশন

#### ১. ক্লায়েন্ট থেকে মেসেজ পাঠানো

```javascript
{
  "type": "CHAT_MESSAGE",
  "messageId": "msg_abc123",        // ইউনিক আইডেন্টিফায়ার
  "payload": {
    "sender": "Shakil",
    "text": "হ্যালো, কেমন আছেন?",
    "roomId": "room_456"
  },
  "metadata": {
    "timestamp": "2026-01-31T12:00:00Z",
    "requiresAck": true               // ACK প্রয়োজন
  }
}
```

#### ২. সার্ভার থেকে ACK রেসপন্স

```javascript
{
  "type": "ACK",
  "messageId": "msg_abc123",        // একই আইডি রিটার্ন
  "status": "DELIVERED",            // অথবা: RECEIVED, PROCESSED, FAILED
  "metadata": {
    "timestamp": "2026-01-31T12:00:01Z",
    "serverProcessedAt": "2026-01-31T12:00:00.500Z"
  }
}
```

#### ৩. ACK স্ট্যাটাস টাইপ

| স্ট্যাটাস   | বর্ণনা                     | ব্যবহার                 |
| ----------- | -------------------------- | ----------------------- |
| `RECEIVED`  | সার্ভার মেসেজ রিসিভ করেছে  | প্রাথমিক নিশ্চিতকরণ     |
| `DELIVERED` | মেসেজ গন্তব্যে পৌঁছেছে     | চ্যাট অ্যাপে একক টিক    |
| `PROCESSED` | সার্ভার মেসেজ প্রসেস করেছে | ডাটাবেসে সেভ সম্পন্ন    |
| `READ`      | রিসিভার মেসেজ পড়েছে       | WhatsApp ব্লু টিক       |
| `FAILED`    | মেসেজ ডেলিভার ব্যর্থ       | এরর হ্যান্ডলিং প্রয়োজন |

---

### 💻 সম্পূর্ণ ইমপ্লিমেন্টেশন

#### ক্লায়েন্ট সাইড (JavaScript)

```javascript
class ReliableWebSocketClient {
  constructor(url) {
    this.socket = new WebSocket(url);
    this.pendingMessages = new Map(); // ACK-এর জন্য অপেক্ষমান মেসেজ
    this.ackTimeout = 5000; // 5 সেকেন্ড টাইমআউট

    this.setupEventHandlers();
  }

  setupEventHandlers() {
    this.socket.onmessage = (event) => {
      const envelope = JSON.parse(event.data);

      if (envelope.type === 'ACK') {
        this.handleAck(envelope);
      } else {
        // অন্যান্য মেসেজ হ্যান্ডল করা
        this.handleMessage(envelope);
      }
    };
  }

  // মেসেজ পাঠানো (ACK সহ)
  sendMessage(type, payload) {
    const messageId = this.generateMessageId();

    const envelope = {
      type,
      messageId,
      payload,
      metadata: {
        timestamp: new Date().toISOString(),
        requiresAck: true,
      },
    };

    // পেন্ডিং লিস্টে যোগ করা
    this.pendingMessages.set(messageId, {
      envelope,
      sentAt: Date.now(),
      retryCount: 0,
    });

    // মেসেজ পাঠানো
    this.socket.send(JSON.stringify(envelope));
    console.log(`📤 মেসেজ পাঠানো: ${messageId}`);

    // টাইমআউট সেট করা
    this.setAckTimeout(messageId);

    return messageId;
  }

  // ACK টাইমআউট সেট করা
  setAckTimeout(messageId) {
    setTimeout(() => {
      if (this.pendingMessages.has(messageId)) {
        console.warn(`⏰ ACK টাইমআউট: ${messageId}`);
        this.handleAckTimeout(messageId);
      }
    }, this.ackTimeout);
  }

  // ACK হ্যান্ডল করা
  handleAck(envelope) {
    const { messageId, status } = envelope;

    if (this.pendingMessages.has(messageId)) {
      const pending = this.pendingMessages.get(messageId);
      const latency = Date.now() - pending.sentAt;

      console.log(
        `✅ ACK প্রাপ্ত: ${messageId} (${latency}ms) - স্ট্যাটাস: ${status}`,
      );

      // সফল হলে পেন্ডিং লিস্ট থেকে সরানো
      if (status === 'DELIVERED' || status === 'PROCESSED') {
        this.pendingMessages.delete(messageId);
        this.onMessageDelivered(messageId, status);
      } else if (status === 'FAILED') {
        this.onMessageFailed(messageId);
      }
    }
  }

  // ACK টাইমআউট হ্যান্ডল করা
  handleAckTimeout(messageId) {
    const pending = this.pendingMessages.get(messageId);

    if (pending.retryCount < 3) {
      // রিট্রাই করা
      pending.retryCount++;
      console.log(
        `🔄 রিট্রাই করা হচ্ছে (${pending.retryCount}/3): ${messageId}`,
      );

      this.socket.send(JSON.stringify(pending.envelope));
      this.setAckTimeout(messageId);
    } else {
      // সর্বোচ্চ রিট্রাই পেরিয়ে গেছে
      console.error(`❌ মেসেজ পাঠানো ব্যর্থ: ${messageId}`);
      this.pendingMessages.delete(messageId);
      this.onMessageFailed(messageId);
    }
  }

  // ইউনিক মেসেজ আইডি জেনারেট করা
  generateMessageId() {
    return `msg_${Date.now()}_${Math.random().toString(36).substr(2, 9)}`;
  }

  // কলব্যাক ফাংশন
  onMessageDelivered(messageId, status) {
    // UI আপডেট করা (যেমন: টিক মার্ক দেখানো)
    console.log(`🎉 মেসেজ ডেলিভার সফল: ${messageId}`);
  }

  onMessageFailed(messageId) {
    // UI-তে এরর দেখানো
    console.error(`💥 মেসেজ ব্যর্থ: ${messageId}`);
  }

  handleMessage(envelope) {
    // সাধারণ মেসেজ প্রসেস করা
    console.log('📨 মেসেজ প্রাপ্ত:', envelope);
  }
}

// ব্যবহার
const client = new ReliableWebSocketClient('ws://localhost:8080');

// চ্যাট মেসেজ পাঠানো
const msgId = client.sendMessage('CHAT_MESSAGE', {
  sender: 'Shakil',
  text: 'হ্যালো সবাই!',
  roomId: 'room_123',
});
```

---

#### সার্ভার সাইড (Node.js)

```javascript
import { WebSocketServer, WebSocket } from 'ws';

const wss = new WebSocketServer({ port: 8080 });

wss.on('connection', (socket) => {
  console.log('🔌 নতুন ক্লায়েন্ট সংযুক্ত');

  socket.on('message', async (rawData) => {
    try {
      const envelope = JSON.parse(rawData.toString());

      // মেসেজ প্রসেস করা
      await processMessage(envelope, socket);

      // ACK পাঠানো (যদি প্রয়োজন হয়)
      if (envelope.metadata?.requiresAck) {
        sendAck(socket, envelope.messageId, 'DELIVERED');
      }
    } catch (error) {
      console.error('❌ মেসেজ প্রসেস এরর:', error);

      // এরর ACK পাঠানো
      if (envelope?.messageId) {
        sendAck(socket, envelope.messageId, 'FAILED', error.message);
      }
    }
  });
});

// মেসেজ প্রসেস করা
async function processMessage(envelope, socket) {
  console.log(`📩 মেসেজ প্রসেস করা হচ্ছে: ${envelope.messageId}`);

  // মেসেজ টাইপ অনুযায়ী হ্যান্ডলিং
  switch (envelope.type) {
    case 'CHAT_MESSAGE':
      // ডাটাবেসে সেভ করা
      await saveChatMessage(envelope.payload);

      // অন্য ক্লায়েন্টদের কাছে ব্রডকাস্ট
      broadcastMessage(envelope);
      break;

    case 'TYPING_START':
      broadcastTypingIndicator(envelope.payload);
      break;

    default:
      console.warn('⚠️ অজানা মেসেজ টাইপ:', envelope.type);
  }
}

// ACK পাঠানো
function sendAck(socket, messageId, status, errorMessage = null) {
  const ackEnvelope = {
    type: 'ACK',
    messageId,
    status,
    metadata: {
      timestamp: new Date().toISOString(),
      serverProcessedAt: new Date().toISOString(),
    },
  };

  if (errorMessage) {
    ackEnvelope.payload = { error: errorMessage };
  }

  socket.send(JSON.stringify(ackEnvelope));
  console.log(`✅ ACK পাঠানো: ${messageId} - ${status}`);
}

// চ্যাট মেসেজ সেভ করা (ডামি ফাংশন)
async function saveChatMessage(payload) {
  // ডাটাবেস অপারেশন
  return new Promise((resolve) => {
    setTimeout(() => {
      console.log('💾 মেসেজ ডাটাবেসে সেভ হয়েছে');
      resolve();
    }, 100);
  });
}

// মেসেজ ব্রডকাস্ট করা
function broadcastMessage(envelope) {
  wss.clients.forEach((client) => {
    if (client.readyState === WebSocket.OPEN) {
      client.send(
        JSON.stringify({
          type: envelope.type,
          payload: envelope.payload,
          metadata: {
            ...envelope.metadata,
            broadcastedAt: new Date().toISOString(),
          },
        }),
      );
    }
  });
}

console.log('✅ WebSocket সার্ভার চালু: ws://localhost:8080');
```

---

### 🔄 রিট্রাই লজিক (Retry Mechanism)

ACK না পেলে স্বয়ংক্রিয় রিট্রাই সিস্টেম:

```javascript
class RetryConfig {
  maxRetries = 3; // সর্বোচ্চ রিট্রাই সংখ্যা
  initialTimeout = 5000; // প্রথম টাইমআউট (5 সেকেন্ড)
  backoffMultiplier = 1.5; // প্রতিটি রিট্রাইতে টাইমআউট বৃদ্ধি

  getTimeout(retryCount) {
    return this.initialTimeout * Math.pow(this.backoffMultiplier, retryCount);
  }
}

// Exponential Backoff উদাহরণ:
// 1st retry: 5 seconds
// 2nd retry: 7.5 seconds
// 3rd retry: 11.25 seconds
```

#### উন্নত রিট্রাই স্ট্র্যাটেজি

```javascript
class AdvancedRetryHandler {
  constructor(config = {}) {
    this.config = {
      maxRetries: config.maxRetries || 3,
      baseDelay: config.baseDelay || 1000,
      maxDelay: config.maxDelay || 30000,
      strategy: config.strategy || 'exponential', // 'linear', 'exponential', 'fibonacci'
    };
  }

  calculateDelay(retryCount) {
    let delay;

    switch (this.config.strategy) {
      case 'linear':
        delay = this.config.baseDelay * (retryCount + 1);
        break;

      case 'exponential':
        delay = this.config.baseDelay * Math.pow(2, retryCount);
        break;

      case 'fibonacci':
        delay = this.config.baseDelay * this.fibonacci(retryCount + 1);
        break;

      default:
        delay = this.config.baseDelay;
    }

    // সর্বোচ্চ ডিলের চেয়ে বেশি না হওয়া
    return Math.min(delay, this.config.maxDelay);
  }

  fibonacci(n) {
    if (n <= 1) return 1;
    let a = 1,
      b = 1;
    for (let i = 2; i < n; i++) {
      [a, b] = [b, a + b];
    }
    return b;
  }
}

// ব্যবহার
const retryHandler = new AdvancedRetryHandler({
  strategy: 'exponential',
  maxRetries: 5,
  baseDelay: 1000,
});

console.log(retryHandler.calculateDelay(0)); // 1000ms
console.log(retryHandler.calculateDelay(1)); // 2000ms
console.log(retryHandler.calculateDelay(2)); // 4000ms
console.log(retryHandler.calculateDelay(3)); // 8000ms
```

---

### 📊 ACK ট্র্যাকিং এবং মনিটরিং

```javascript
class AckMonitor {
  constructor() {
    this.stats = {
      sent: 0,
      delivered: 0,
      failed: 0,
      avgLatency: 0,
      latencies: [],
    };
  }

  recordSent() {
    this.stats.sent++;
  }

  recordDelivered(latency) {
    this.stats.delivered++;
    this.stats.latencies.push(latency);
    this.updateAvgLatency();
  }

  recordFailed() {
    this.stats.failed++;
  }

  updateAvgLatency() {
    const sum = this.stats.latencies.reduce((a, b) => a + b, 0);
    this.stats.avgLatency = sum / this.stats.latencies.length;
  }

  getDeliveryRate() {
    return ((this.stats.delivered / this.stats.sent) * 100).toFixed(2);
  }

  getReport() {
    return {
      totalSent: this.stats.sent,
      delivered: this.stats.delivered,
      failed: this.stats.failed,
      deliveryRate: `${this.getDeliveryRate()}%`,
      avgLatency: `${this.stats.avgLatency.toFixed(2)}ms`,
      minLatency: `${Math.min(...this.stats.latencies)}ms`,
      maxLatency: `${Math.max(...this.stats.latencies)}ms`,
    };
  }
}

// ব্যবহার
const monitor = new AckMonitor();
monitor.recordSent();
monitor.recordDelivered(150); // 150ms লেটেন্সি

console.log('📊 ACK রিপোর্ট:', monitor.getReport());
// Output:
// {
//   totalSent: 1,
//   delivered: 1,
//   failed: 0,
//   deliveryRate: '100.00%',
//   avgLatency: '150.00ms',
//   minLatency: '150ms',
//   maxLatency: '150ms'
// }
```

---

### 🎨 UI ইন্টিগ্রেশন উদাহরণ

#### React-এ ACK স্ট্যাটাস দেখানো

```jsx
import React, { useState, useEffect } from 'react';

function ChatMessage({ message, messageId }) {
  const [status, setStatus] = useState('SENDING'); // SENDING, DELIVERED, READ, FAILED

  useEffect(() => {
    // ACK স্ট্যাটাস আপডেট শুনুন
    const handleAck = (ackData) => {
      if (ackData.messageId === messageId) {
        setStatus(ackData.status);
      }
    };

    // ইভেন্ট লিসেনার যোগ করুন
    window.addEventListener('ack-received', handleAck);

    return () => {
      window.removeEventListener('ack-received', handleAck);
    };
  }, [messageId]);

  const getStatusIcon = () => {
    switch (status) {
      case 'SENDING':
        return '🕐'; // ঘড়ি
      case 'DELIVERED':
        return '✓'; // একক টিক
      case 'READ':
        return '✓✓'; // ডাবল টিক (নীল)
      case 'FAILED':
        return '⚠️'; // সতর্কতা
      default:
        return '';
    }
  };

  return (
    <div className='chat-message'>
      <p>{message}</p>
      <span className={`status ${status.toLowerCase()}`}>
        {getStatusIcon()}
      </span>
    </div>
  );
}
```

---

### ✅ বেস্ট প্র্যাকটিস

#### ১. ইউনিক মেসেজ আইডি জেনারেশন

```javascript
// ✅ ভালো - টাইমস্ট্যাম্প + র‍্যান্ডম
const generateId = () => `msg_${Date.now()}_${crypto.randomUUID()}`;

// ❌ খারাপ - শুধু র‍্যান্ডম (collision ঝুঁকি)
const badId = () => Math.random().toString();
```

#### ২. টাইমআউট কনফিগারেশন

```javascript
// নেটওয়ার্ক কন্ডিশন অনুযায়ী টাইমআউট সেট করুন
const timeouts = {
  fast: 2000, // WiFi/4G
  normal: 5000, // সাধারণ মোবাইল
  slow: 10000, // 2G/দুর্বল সংযোগ
};
```

#### ৩. ব্যাচ ACK (একাধিক মেসেজের জন্য)

```javascript
// একসাথে একাধিক মেসেজের ACK পাঠানো
{
  "type": "BATCH_ACK",
  "messageIds": ["msg_1", "msg_2", "msg_3"],
  "status": "DELIVERED",
  "metadata": {
    "timestamp": "2026-01-31T12:00:00Z"
  }
}
```

#### ৪. পার্সিসটেন্স (অফলাইন সাপোর্ট)

```javascript
// LocalStorage-এ পেন্ডিং মেসেজ সেভ করা
class PersistentMessageQueue {
  save(messageId, envelope) {
    const queue = this.getQueue();
    queue[messageId] = envelope;
    localStorage.setItem('pending_messages', JSON.stringify(queue));
  }

  getQueue() {
    return JSON.parse(localStorage.getItem('pending_messages') || '{}');
  }

  remove(messageId) {
    const queue = this.getQueue();
    delete queue[messageId];
    localStorage.setItem('pending_messages', JSON.stringify(queue));
  }

  // অফলাইন থেকে অনলাইনে আসলে পুনরায় পাঠানো
  resendPending(socket) {
    const queue = this.getQueue();
    Object.values(queue).forEach((envelope) => {
      socket.send(JSON.stringify(envelope));
    });
  }
}
```

---

### 🔒 সিকিউরিটি বিবেচনা

```javascript
// ACK স্পুফিং প্রতিরোধ
class SecureAckHandler {
  constructor() {
    this.messageHashes = new Map();
  }

  // মেসেজ পাঠানোর সময় হ্যাশ তৈরি
  createHash(messageId, payload) {
    const data = `${messageId}:${JSON.stringify(payload)}:${SECRET_KEY}`;
    return crypto.subtle.digest('SHA-256', new TextEncoder().encode(data));
  }

  // ACK ভেরিফাই করা
  verifyAck(messageId, hash) {
    const expectedHash = this.messageHashes.get(messageId);
    return expectedHash === hash;
  }
}
```

---

### 📈 পারফরম্যান্স অপটিমাইজেশন

```javascript
// থ্রটলিং - অতিরিক্ত ACK রিকোয়েস্ট প্রতিরোধ
class ThrottledAckHandler {
  constructor(interval = 100) {
    this.interval = interval;
    this.lastSent = 0;
    this.pendingAcks = [];
  }

  scheduleAck(messageId) {
    this.pendingAcks.push(messageId);

    const now = Date.now();
    if (now - this.lastSent >= this.interval) {
      this.sendBatchAck();
      this.lastSent = now;
    } else {
      setTimeout(() => this.sendBatchAck(), this.interval);
    }
  }

  sendBatchAck() {
    if (this.pendingAcks.length === 0) return;

    const batchAck = {
      type: 'BATCH_ACK',
      messageIds: [...this.pendingAcks],
      status: 'DELIVERED',
    };

    socket.send(JSON.stringify(batchAck));
    this.pendingAcks = [];
  }
}
```

---

## 📡 মেসেজ ব্রোকার (Message Broker)

যখন আপনার WebSocket অ্যাপ্লিকেশন **একক সার্ভার থেকে মাল্টি-সার্ভার আর্কিটেকচারে** রূপান্তরিত হয়, তখন **মেসেজ ব্রোকার** অপরিহার্য হয়ে পড়ে। এটি **ডিস্ট্রিবিউটেড সিস্টেমে স্কেলেবিলিটি এবং রিয়েল-টাইম কমিউনিকেশন** নিশ্চিত করার মূল চাবিকাঠি।

মেসেজ ব্রোকার হলো একটি **মধ্যস্থতাকারী সফটওয়্যার** যা একাধিক সার্ভারের মধ্যে মেসেজ রাউটিং, ডিস্ট্রিবিউশন এবং সিঙ্ক্রোনাইজেশন পরিচালনা করে।

---

### 🤔 সমস্যা: একাধিক সার্ভারে মেসেজ সিঙ্ক্রোনাইজেশন

#### দৃশ্যকল্প (Scenario)

আপনার চ্যাট অ্যাপ্লিকেশন জনপ্রিয় হয়ে উঠেছে এবং একটি সার্ভার দিয়ে আর কুলাচ্ছে না। আপনি **দুটি WebSocket সার্ভার** চালু করেছেন:

```
┌──────────────┐                    ┌──────────────┐
│  Server A    │                    │  Server B    │
│              │                    │              │
│  User-1 👤   │                    │  User-2 👤   │
│  User-3 👤   │                    │  User-4 👤   │
└──────────────┘                    └──────────────┘
```

**সমস্যা:**

- User-1 (Server A-তে) একটি মেসেজ পাঠায়
- Server A শুধুমাত্র তার নিজের ক্লায়েন্টদের (User-1, User-3) কাছে মেসেজ পাঠাতে পারে
- **Server B-এর ক্লায়েন্টরা (User-2, User-4) মেসেজ পাবে না!** ❌

#### সমাধান: Message Broker

```
┌──────────────┐                    ┌──────────────┐
│  Server A    │◄──────────────────►│  Server B    │
│              │                    │              │
│  User-1 👤   │                    │  User-2 👤   │
│  User-3 👤   │                    │  User-4 👤   │
└──────┬───────┘                    └──────┬───────┘
       │                                   │
       │         ┌─────────────────┐       │
       └────────►│ Message Broker  │◄──────┘
                 │   (Redis/Kafka) │
                 └─────────────────┘
```

মেসেজ ব্রোকার সার্ভারগুলোর মধ্যে **সেতু হিসেবে কাজ করে**, যাতে সব সার্ভারের সব ক্লায়েন্ট মেসেজ পায়। ✅

---

### 🔄 Pub/Sub মডেল (Publish/Subscribe Pattern)

মেসেজ ব্রোকার সাধারণত **Pub/Sub (Publisher/Subscriber)** প্যাটার্নে কাজ করে:

#### কীভাবে কাজ করে:

**১. Subscribe (সাবস্ক্রিপশন):**

```javascript
// সব সার্ভার একটি চ্যানেলে সাবস্ক্রাইব করে
Server A → Subscribe to "chat-room-123"
Server B → Subscribe to "chat-room-123"
Server C → Subscribe to "chat-room-123"
```

**২. Publish (প্রকাশনা):**

```javascript
// যেকোনো সার্ভার মেসেজ পাবলিশ করে
Server A → Publish to "chat-room-123": { text: "Hello!" }
```

**৩. Distribution (বিতরণ):**

```javascript
// ব্রোকার সব সাবস্ক্রাইবারদের কাছে পাঠায়
Broker → Sends to Server A, Server B, Server C
```

**৪. Broadcast (সম্প্রচার):**

```javascript
// প্রতিটি সার্ভার তার ক্লায়েন্টদের কাছে পাঠায়
Server A → Broadcasts to User-1, User-3
Server B → Broadcasts to User-2, User-4
```

---

### 📊 জনপ্রিয় মেসেজ ব্রোকার তুলনা

| ব্রোকার          | বৈশিষ্ট্য                                                           | পারফরম্যান্স                  | ব্যবহারের ক্ষেত্র                                         | জটিলতা    |
| ---------------- | ------------------------------------------------------------------- | ----------------------------- | --------------------------------------------------------- | --------- |
| **Redis**        | • In-memory স্টোরেজ<br>• Pub/Sub সাপোর্ট<br>• খুবই দ্রুত            | ⚡⚡⚡⚡⚡<br>(~1M ops/sec)   | • রিয়েল-টাইম চ্যাট<br>• লাইভ নোটিফিকেশন<br>• গেমিং অ্যাপ | 🟢 সহজ    |
| **RabbitMQ**     | • Message persistence<br>• Complex routing<br>• Guaranteed delivery | ⚡⚡⚡⚡<br>(~50K msgs/sec)   | • ফিনান্সিয়াল সিস্টেম<br>• E-commerce<br>• Task queues   | 🟡 মাঝারি |
| **Apache Kafka** | • High throughput<br>• Event streaming<br>• Data retention          | ⚡⚡⚡⚡⚡<br>(~1M msgs/sec)  | • বড় স্কেলের অ্যাপ<br>• লগ অ্যাগ্রিগেশন<br>• IoT সিস্টেম | 🔴 জটিল   |
| **NATS**         | • Lightweight<br>• Cloud-native<br>• Low latency                    | ⚡⚡⚡⚡⚡<br>(~11M msgs/sec) | • Microservices<br>• Cloud apps<br>• IoT                  | 🟢 সহজ    |

---

### 🎯 কখন কোন ব্রোকার ব্যবহার করবেন?

| পরিস্থিতি                   | প্রস্তাবিত ব্রোকার | কারণ                            |
| --------------------------- | ------------------ | ------------------------------- |
| **ছোট-মাঝারি চ্যাট অ্যাপ**  | Redis              | সহজ, দ্রুত, কম রিসোর্স          |
| **এন্টারপ্রাইজ মেসেজিং**    | RabbitMQ           | নির্ভরযোগ্যতা, কমপ্লেক্স রাউটিং |
| **বিশাল স্কেলের স্ট্রিমিং** | Apache Kafka       | উচ্চ থ্রুপুট, ডেটা রিটেনশন      |
| **মাইক্রোসার্ভিস**          | NATS               | লাইটওয়েট, ক্লাউড-নেটিভ         |
| **IoT অ্যাপ্লিকেশন**        | MQTT (Mosquitto)   | লো-ব্যান্ডউইথ অপটিমাইজড         |

---

## 🎥 WebRTC (Web Real-Time Communication)

WebSocket যদি হয় **টেক্সট-ভিত্তিক রিয়েল-টাইম কমিউনিকেশনের** জন্য, তাহলে **WebRTC হলো অডিও, ভিডিও এবং ডেটা স্ট্রিমিংয়ের** শক্তিশালী প্রযুক্তি। এটি ব্রাউজার এবং মোবাইল অ্যাপ্লিকেশনে **প্লাগইন ছাড়াই** সরাসরি পিয়ার-টু-পিয়ার কমিউনিকেশন সক্ষম করে।

WebRTC-এর মূল বৈশিষ্ট্য হলো এটি **Peer-to-Peer (P2P)** মডেলে কাজ করে, যেখানে দুই ক্লায়েন্ট সরাসরি একে অপরের সাথে সংযুক্ত হয়—মাঝখানে সার্ভারের মাধ্যমে ডেটা রিলে করার প্রয়োজন হয় না।

---

### 🤔 কেন WebRTC? (মূল পার্থক্য)

#### WebSocket vs WebRTC: আর্কিটেকচারাল পার্থক্য

**WebSocket মডেল (Client-Server):**

```
Client A  ──►  Server  ──►  Client B
   👤            🖥️           👤
  (ভিডিও)   (রিলে পয়েন্ট)   (ভিডিও)
```

- সমস্ত ডেটা সার্ভারের মধ্য দিয়ে যায়
- সার্ভারে **উচ্চ ব্যান্ডউইথ খরচ**
- **বেশি লেটেন্সি** (দুটি হপ: client→server→client)
- স্কেলিং কঠিন এবং ব্যয়বহুল

**WebRTC মডেল (Peer-to-Peer):**

```
Client A  ◄──────────────────►  Client B
   👤         (সরাসরি সংযোগ)        👤
  (ভিডিও)                         (ভিডিও)
```

- ডেটা সরাসরি পিয়ারদের মধ্যে প্রবাহিত হয়
- **সার্ভার ব্যান্ডউইথ সাশ্রয়** (শুধু সিগন্যালিংয়ে ব্যবহৃত)
- **অত্যন্ত কম লেটেন্সি** (এক হপ: client→client)
- স্বয়ংক্রিয়ভাবে স্কেলেবল

#### সুবিধাসমূহ:

✅ **আল্ট্রা-লো লেটেন্সি:** রিয়েল-টাইম ভিডিও/অডিও কমিউনিকেশনের জন্য আদর্শ  
✅ **ব্যান্ডউইথ দক্ষতা:** সার্ভারে মিডিয়া রিলে করার প্রয়োজন নেই  
✅ **এন্ড-টু-এন্ড এনক্রিপশন:** DTLS এবং SRTP দিয়ে সুরক্ষিত  
✅ **ক্রস-প্ল্যাটফর্ম:** সব আধুনিক ব্রাউজার এবং মোবাইল অ্যাপ সমর্থন করে  
✅ **কস্ট ইফেক্টিভ:** সার্ভার ইনফ্রাস্ট্রাকচার খরচ কম

---

### ⚙️ WebRTC কীভাবে কাজ করে? (তিনটি স্তম্ভ)

দুটি ব্রাউজার যারা একে অপরকে চেনে না এবং সম্ভবত ফায়ারওয়াল/NAT-এর পেছনে আছে, তারা কীভাবে সরাসরি সংযুক্ত হয়? WebRTC তিনটি প্রধান পর্যায়ে কাজ করে:

---

#### 🔔 ১. সিগন্যালিং (Signaling)

**উদ্দেশ্য:** দুটি পিয়ার একে অপরের সম্পর্কে তথ্য বিনিময় করা

সিগন্যালিং হলো **সংযোগ স্থাপনের প্রাথমিক হ্যান্ডশেক প্রক্রিয়া**। এখানে পিয়াররা বিনিময় করে:

**📄 SDP (Session Description Protocol):**

- মিডিয়া ক্যাপাবিলিটি (কোডেক, রেজোলিউশন, ফরম্যাট)
- নেটওয়ার্ক তথ্য (আইপি, পোর্ট)
- সেশন মেটাডেটা

**🧊 ICE Candidates:**

- সম্ভাব্য নেটওয়ার্ক পথসমূহ
- পাবলিক এবং প্রাইভেট আইপি ঠিকানা
- বিভিন্ন প্রোটোকল (UDP, TCP, TLS)

**গুরুত্বপূর্ণ:** WebRTC নিজে সিগন্যালিং করে না! আপনাকে নিজের সিগন্যালিং মেকানিজম ইমপ্লিমেন্ট করতে হবে (সাধারণত **WebSocket** ব্যবহার করে)।

---

#### 🌐 ২. NAT ট্রাভার্সাল (STUN/TURN সার্ভার)

**সমস্যা:** বেশিরভাগ ডিভাইস রাউটার/ফায়ারওয়ালের পেছনে থাকে এবং সরাসরি পৌঁছানো যায় না।

**সমাধান:**

**🔍 STUN সার্ভার (Session Traversal Utilities for NAT):**

- **কাজ:** ক্লায়েন্টের পাবলিক আইপি অ্যাড্রেস এবং পোর্ট আবিষ্কার করা
- **কখন ব্যবহৃত:** সাধারণ NAT পরিস্থিতিতে (80-90% ক্ষেত্রে)
- **ব্যান্ডউইথ:** খুবই কম (শুধু আইপি ডিসকভারি)
- **উদাহরণ:** Google STUN সার্ভার (`stun:stun.l.google.com:19302`)

**🔄 TURN সার্ভার (Traversal Using Relays around NAT):**

- **কাজ:** সরাসরি সংযোগ ব্যর্থ হলে মিডিয়া রিলে করা
- **কখন ব্যবহৃত:** কঠিন ফায়ারওয়াল/সিমেট্রিক NAT (10-20% ক্ষেত্রে)
- **ব্যান্ডউইথ:** উচ্চ (সম্পূর্ণ মিডিয়া স্ট্রিম রিলে)
- **খরচ:** ব্যান্ডউইথ খরচের কারণে বেশি

**ICE (Interactive Connectivity Establishment):**

- STUN এবং TURN-এর সমন্বয়ে সর্বোত্তম সংযোগ পথ খুঁজে বের করে
- একাধিক সম্ভাব্য পথ পরীক্ষা করে
- সবচেয়ে দ্রুত এবং নির্ভরযোগ্য রুট নির্বাচন করে

---

#### 🔗 ৩. পিয়ার-টু-পিয়ার কানেকশন

একবার সিগন্যালিং এবং NAT ট্রাভার্সাল সম্পন্ন হলে:

✅ **সরাসরি মিডিয়া স্ট্রিম** শুরু হয়  
✅ **DTLS** (Datagram Transport Layer Security) দিয়ে এনক্রিপ্ট  
✅ **SRTP** (Secure Real-time Transport Protocol) দিয়ে মিডিয়া ট্রান্সপোর্ট  
✅ **অডিও, ভিডিও এবং ডেটা চ্যানেল** সব একসাথে চলতে পারে

---

### 📊 WebSocket বনাম WebRTC: বিস্তারিত তুলনা

| বৈশিষ্ট্য             | WebSocket                            | WebRTC                                               |
| --------------------- | ------------------------------------ | ---------------------------------------------------- |
| **আর্কিটেকচার**       | Client-Server (কেন্দ্রীভূত)          | Peer-to-Peer (বিকেন্দ্রীকৃত)                         |
| **ডেটা টাইপ**         | টেক্সট, JSON, ছোট বাইনারি            | হাই-কোয়ালিটি অডিও, ভিডিও, বড় ফাইল                  |
| **লেটেন্সি**          | মিডিয়াম (50-200ms)                  | আল্ট্রা-লো (10-50ms)                                 |
| **ব্যান্ডউইথ খরচ**    | সার্ভারে উচ্চ                        | সার্ভারে নগণ্য (শুধু সিগন্যালিং)                     |
| **সিকিউরিটি**         | TLS/SSL (wss://)                     | DTLS + SRTP (বিল্ট-ইন)                               |
| **স্কেলেবিলিটি**      | সার্ভার ক্যাপাসিটির উপর নির্ভরশীল    | স্বয়ংক্রিয়ভাবে স্কেলেবল                            |
| **ব্যবহারের ক্ষেত্র** | চ্যাট, নোটিফিকেশন, লাইভ আপডেট, গেমিং | ভিডিও/অডিও কল, স্ক্রিন শেয়ারিং, P2P ফাইল ট্রান্সফার |
| **ব্রাউজার সাপোর্ট**  | সব আধুনিক ব্রাউজার                   | সব আধুনিক ব্রাউজার                                   |
| **NAT ট্রাভার্সাল**   | প্রয়োজন নেই                         | STUN/TURN সার্ভার প্রয়োজন                           |
| **প্রোটোকল**          | TCP                                  | UDP (প্রধান), TCP (ফলব্যাক)                          |

---

## 📡 Server-Sent Events (SSE)

**Server-Sent Events (SSE)** হলো রিয়েল-টাইম ডেটা ট্রান্সমিশনের একটি **সরল, কার্যকর এবং সাশ্রয়ী** পদ্ধতি যা **HTTP প্রোটোকলের উপর ভিত্তি করে** তৈরি। WebSocket যেখানে **দ্বিমুখী (bi-directional)** কমিউনিকেশন চ্যানেল, SSE সেখানে একটি **একমুখী (unidirectional)** চ্যানেল—যেখানে **শুধুমাত্র সার্ভার ক্লায়েন্টকে ডেটা পুশ করতে পারে**।

SSE বিশেষভাবে কার্যকর সেসব ক্ষেত্রে যেখানে ক্লায়েন্টকে শুধু আপডেট রিসিভ করতে হয়, কিন্তু সার্ভারে ফিরতি ডেটা পাঠানোর প্রয়োজন নেই—যেমন **লাইভ নোটিফিকেশন, স্টক প্রাইস আপডেট, নিউজ ফিড, রিয়েল-টাইম ড্যাশবোর্ড** ইত্যাদি।

---

### 🤔 SSE কেন এবং কখন?

#### উপযুক্ত ব্যবহারের ক্ষেত্র:

✅ **লাইভ নোটিফিকেশন সিস্টেম** (Facebook, Twitter)  
✅ **স্টক মার্কেট/ক্রিপ্টো প্রাইস আপডেট**  
✅ **নিউজ ফিড এবং সোশ্যাল মিডিয়া আপডেট**  
✅ **সার্ভার-সাইড লগ স্ট্রিমিং**  
✅ **স্পোর্টস স্কোর লাইভ আপডেট**  
✅ **মনিটরিং ড্যাশবোর্ড (সিস্টেম মেট্রিক্স, অ্যানালিটিক্স)**  
✅ **প্রগ্রেস ইন্ডিকেটর (ফাইল আপলোড, ডেটা প্রসেসিং)**

#### অনুপযুক্ত ক্ষেত্র:

❌ **চ্যাট অ্যাপ্লিকেশন** (দ্বিমুখী যোগাযোগ প্রয়োজন)  
❌ **মাল্টিপ্লেয়ার গেমিং** (দ্রুত দ্বিমুখী ডেটা এক্সচেঞ্জ)  
❌ **কোলাবরেটিভ এডিটিং** (রিয়েল-টাইম সিঙ্ক প্রয়োজন)  
❌ **বাইনারি ফাইল ট্রান্সফার** (SSE শুধু টেক্সট সাপোর্ট করে)

---

### ⚙️ SSE কীভাবে কাজ করে?

SSE একটি **লং-লিভড HTTP সংযোগ** যা সার্ভার ইচ্ছামত খোলা রাখতে পারে। প্রক্রিয়াটি:

```
┌──────────┐                           ┌──────────┐
│  Client  │                           │  Server  │
└─────┬────┘                           └─────┬────┘
      │                                      │
      │ 1️⃣ HTTP GET Request                 │
      │ (EventSource connection)            │
      │─────────────────────────────────────►│
      │                                      │
      │ 2️⃣ Response Headers                 │
      │ (Content-Type: text/event-stream)   │
      │◄─────────────────────────────────────│
      │                                      │
      │ 3️⃣ Connection stays OPEN            │
      │◄═════════════════════════════════════│
      │                                      │
      │ 4️⃣ Server pushes data continuously  │
      │      data: {...}\n\n                │
      │◄─────────────────────────────────────│
      │      data: {...}\n\n                │
      │◄─────────────────────────────────────│
      │      data: {...}\n\n                │
      │◄─────────────────────────────────────│
      │                                      │
```

#### মূল বৈশিষ্ট্য:

🔹 **HTTP-ভিত্তিক:** স্ট্যান্ডার্ড HTTP/1.1 বা HTTP/2 ব্যবহার করে  
🔹 **Persistent Connection:** সংযোগ দীর্ঘসময় খোলা থাকে  
🔹 **Server Push:** সার্ভার যেকোনো সময় ডেটা পুশ করতে পারে  
🔹 **Text-Only:** শুধুমাত্র UTF-8 টেক্সট ডেটা সমর্থিত  
🔹 **Event-Driven:** ব্রাউজারের EventSource API ব্যবহার করে

---

### 📊 WebSocket বনাম SSE: বিস্তারিত তুলনা

| বৈশিষ্ট্য                  | WebSocket                       | SSE (Server-Sent Events)                       |
| -------------------------- | ------------------------------- | ---------------------------------------------- |
| **যোগাযোগ মডেল**           | দ্বিমুখী (Full-Duplex)          | একমুখী (Server → Client only)                  |
| **প্রোটোকল**               | কাস্টম বাইনারি প্রোটোকল (ws://) | স্ট্যান্ডার্ড HTTP/HTTPS                       |
| **ডেটা টাইপ**              | টেক্সট এবং বাইনারি উভয়ই        | শুধুমাত্র টেক্সট (UTF-8)                       |
| **অটো রিকানেকশন**          | ম্যানুয়াল ইমপ্লিমেন্ট করতে হয় | ব্রাউজার স্বয়ংক্রিয়ভাবে করে (3 সেকেন্ড ডিলে) |
| **HTTP/2 মাল্টিপ্লেক্সিং** | সমর্থিত নয়                     | সম্পূর্ণ সমর্থিত                               |
| **ব্যান্ডউইথ খরচ**         | তুলনামূলক বেশি                  | কম (HTTP হেডার কম্প্রেশন)                      |
| **ব্রাউজার সাপোর্ট**       | সব আধুনিক ব্রাউজার              | সব আধুনিক ব্রাউজার (IE ছাড়া)                  |
| **ফায়ারওয়াল/প্রক্সি**    | সমস্যা হতে পারে                 | সমস্যা হয় না (HTTP ব্যবহার করে)               |
| **মেসেজ ফরম্যাট**          | কাস্টম (যেকোনো ফরম্যাট)         | নির্দিষ্ট ফরম্যাট (data: ...\n\n)              |
| **কমপ্লেক্সিটি**           | মাঝারি                          | সহজ                                            |
| **সার্ভার লোড**            | বেশি                            | কম                                             |
| **ব্যবহারের ক্ষেত্র**      | চ্যাট, গেমিং, কোলাবরেশন         | নোটিফিকেশন, লাইভ ফিড, মনিটরিং                  |
| **কানেকশন লিমিট**          | প্রতি ডোমেইনে সীমাহীন           | প্রতি ডোমেইনে 6টি (HTTP/1.1)                   |

---

### ✅ SSE-এর সুবিধা (Advantages)

#### 1️⃣ **সরলতা (Simplicity)**

- WebSocket-এর তুলনায় অনেক সহজ ইমপ্লিমেন্ট করা
- স্ট্যান্ডার্ড HTTP ব্যবহার করে, বিশেষ প্রোটোকল নয়

#### 2️⃣ **স্বয়ংক্রিয় রিকানেকশন (Auto-Reconnection)**

- ব্রাউজার নিজে থেকেই কানেকশন পুনরায় স্থাপন করে
- কোনো অতিরিক্ত কোড লেখার প্রয়োজন নেই

#### 3️⃣ **ফায়ারওয়াল-ফ্রেন্ডলি (Firewall-Friendly)**

- সাধারণ HTTP/HTTPS ব্যবহার করে
- প্রক্সি এবং ফায়ারওয়াল সমস্যা কম

#### 4️⃣ **HTTP/2 সুবিধা**

- HTTP/2 মাল্টিপ্লেক্সিং এর সুবিধা
- একই TCP কানেকশনে একাধিক SSE স্ট্রিম

#### 5️⃣ **লাইটওয়েট (Lightweight)**

- কম সার্ভার রিসোর্স ব্যবহার
- কম ব্যান্ডউইথ খরচ

#### 6️⃣ **ইভেন্ট আইডি সাপোর্ট**

- মেসেজ ট্র্যাকিং এবং রিকভারি সহজ
- নেটওয়ার্ক ইস্যুতে মিসিং ডেটা রিকভার করা যায়

---

### ⚠️ SSE-এর সীমাবদ্ধতা (Limitations)

#### 1️⃣ **একমুখী যোগাযোগ**

- শুধুমাত্র সার্ভার → ক্লায়েন্ট
- ক্লায়েন্ট রিয়েল-টাইমে ডেটা পাঠাতে পারে না

#### 2️⃣ **কানেকশন লিমিট**

- HTTP/1.1: প্রতি ডোমেইনে সর্বোচ্চ 6টি কানেকশন
- সমাধান: HTTP/2 ব্যবহার করুন বা বিভিন্ন সাবডোমেইন

#### 3️⃣ **টেক্সট-অনলি**

- বাইনারি ডেটা সরাসরি পাঠানো যায় না
- সমাধান: Base64 এনকোডিং (কিন্তু ওভারহেড বেশি)

#### 4️⃣ **ব্রাউজার সাপোর্ট**

- Internet Explorer সাপোর্ট করে না
- সমাধান: Polyfill ব্যবহার করুন

#### 5️⃣ **স্কেলিং চ্যালেঞ্জ**

- অনেক সংযুক্ত ক্লায়েন্টের জন্য সার্ভার রিসোর্স প্রয়োজন
- সমাধান: Load balancing এবং clustering

---

### 🎯 SSE vs WebSocket vs Long Polling: কখন কোনটি?

| পরিস্থিতি                   | প্রস্তাবিত প্রযুক্তি | কারণ                     |
| --------------------------- | -------------------- | ------------------------ |
| **একমুখী ডেটা পুশ**         | SSE ✅               | সহজ, কম রিসোর্স          |
| **দ্বিমুখী যোগাযোগ**        | WebSocket ✅         | রিয়েল-টাইম দ্বিমুখী     |
| **পুরানো ব্রাউজার সাপোর্ট** | Long Polling ✅      | সর্বজনীন সাপোর্ট         |
| **লাইভ নোটিফিকেশন**         | SSE ✅               | অটো-রিকানেক্ট, সহজ       |
| **চ্যাট অ্যাপ্লিকেশন**      | WebSocket ✅         | দ্রুত দ্বিমুখী           |
| **স্টক/স্পোর্টস আপডেট**     | SSE ✅               | কম লেটেন্সি, এফিশিয়েন্ট |
| **মাল্টিপ্লেয়ার গেম**      | WebSocket ✅         | আল্ট্রা-লো লেটেন্সি      |
| **ফায়ারওয়াল সমস্যা**      | SSE ✅               | HTTP-ভিত্তিক             |

---

### 📦 জনপ্রিয় SSE লাইব্রেরি

**ব্যাকএন্ড:**

- **Express SSE** - Express.js মিডলওয়্যার
- **SSE-Express** - সিম্পল SSE হ্যান্ডলার
- **Fastify SSE** - Fastify ফ্রেমওয়ার্কের জন্য

**ফ্রন্টএন্ড:**

- **EventSource Polyfill** - IE সাপোর্টের জন্য
- **Reconnecting EventSource** - উন্নত রিকানেক্ট লজিক

---

### Web transport

WebTransport হলো রিয়েল-টাইম কমিউনিকেশনের একদম আধুনিক এবং অত্যাধুনিক (Next-generation) প্রোটোকল। যদি WebSockets হয় একটি পুরনো হাইওয়ে, তবে WebTransport হলো একটি সুপার-ফাস্ট এক্সপ্রেসওয়ে।

এটি মূলত HTTP/3 (QUIC প্রোটোকল) এর ওপর ভিত্তি করে তৈরি করা হয়েছে, যা WebSocket-এর অনেক সীমাবদ্ধতা দূর করে।

১. কেন WebTransport প্রয়োজন? (The "Why")

WebSockets-এর একটি বড় সমস্যা হলো Head-of-Line Blocking। যেহেতু এটি TCP ব্যবহার করে, যদি ইন্টারনেটে একটি প্যাকেট হারিয়ে যায়, তবে তার পরের সব মেসেজ আটকে থাকে যতক্ষণ না ওই প্যাকেটটি আবার ফিরে আসে।

WebTransport এটি সমাধান করে কারণ এটি UDP (via QUIC) ব্যবহার করে। এতে একটি মেসেজ হারিয়ে গেলে অন্য মেসেজগুলো আটকে থাকে না।

২. প্রধান বৈশিষ্ট্যসমূহ (Key Features)

Multiplexing: আপনি একটি কানেকশন দিয়েই অনেকগুলো আলাদা আলাদা স্ট্রিম (Streams) পাঠাতে পারেন। একটি স্ট্রিম ক্র্যাশ করলে অন্যটির কোনো সমস্যা হয় না।

Unreliable Datagrams: লাইভ গেমিং বা ভিডিও স্ট্রিমিংয়ের জন্য সব সময় "গ্যারান্টিড ডেলিভারি" লাগে না। WebTransport আপনাকে "পাঠিয়ে দাও, পৌঁছালে পৌঁছালো" (UDP-like) মোডে ডেটা পাঠাতে দেয়।

Low Latency: এটি WebSockets-এর চেয়েও দ্রুত কানেকশন শুরু করতে পারে (0-RTT Handshake)।

৩. WebTransport বনাম WebSockets বনাম WebRTC

বৈশিষ্ট্য,WebSockets,WebRTC,WebTransport
প্রোটোকল,TCP,UDP (P2P),HTTP/3 (QUIC)
কানেকশন,Client-Server,Peer-to-Peer,Client-Server
নির্ভরযোগ্যতা,১০০% নির্ভরযোগ্য,নির্ভরযোগ্য নয়,নির্ভরযোগ্য + অনির্ভরযোগ্য (উভয়ই)
ব্যবহার,চ্যাট অ্যাপ,ভিডিও কল,"গেমিং, হাই-স্পিড ডেটা স্ট্রিমিং"

৪. কোড উদাহরণ (Client Side)
ব্রাউজারে এটি ব্যবহার করা খুবই সহজ (বর্তমানে সব মডার্ন ব্রাউজার এটি সাপোর্ট করে):

```bash
// WebTransport কানেকশন শুরু
const transport = new WebTransport("https://your-server.com:443/webtransport");

// কানেকশন হওয়ার জন্য অপেক্ষা
await transport.ready;

// ১. নির্ভরযোগ্য স্ট্রিম পাঠানো (Reliable Stream)
const stream = await transport.createSendStream();
const writer = stream.writable.getWriter();
writer.write(new TextEncoder().encode("Hello high-speed world!"));

// ২. অনির্ভরযোগ্য ডেটাগ্রাম পাঠানো (Unreliable Datagram - Fast but risky)
const datagramWriter = transport.datagrams.writable.getWriter();
datagramWriter.write(new TextEncoder().encode("Fast gaming data"));
```

৫. কখন এটি ব্যবহার করবেন?

Cloud Gaming: যেখানে প্রতি মিলি-সেকেন্ড গুরুত্বপূর্ণ।

Live Video Streaming: যেখানে ছোট ল্যাগ হলে সমস্যা নেই কিন্তু ভিডিও চলতে থাকা জরুরি।

IoT: বিশাল পরিমাণ সেন্সর ডেটা দ্রুত সার্ভারে পাঠানোর জন্য।
